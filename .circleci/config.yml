version: 2.1

jobs:
  build: # our first job, named "build"
    docker:
      - image: circleci/ruby:2.7
    steps:
      - checkout # pull down our git code.
      - gem install bundler
      - bundle exec rake

workflow:
  version: 2.1
  build-test:
    jobs:
      - build

